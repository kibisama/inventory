import { styled } from '@mui/material/styles';
import { TreeItem2Content } from '@mui/x-tree-view/TreeItem2';

const StyledTreeItemContent = styled(TreeItem2Content)(({ theme }) => ({
  //   padding: theme.spacing(0, '1rem'),
}));

export default StyledTreeItemContent;
